streamlit==1.58.0
plotly==6.8.0
pandas==3.0.3
httpx==0.28.1
faker==40.22.0

# Phase 2: federated ingestion (FastAPI gateway + edge agent).
# Edge agent posts via httpx (already above) — no `requests` needed.
fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4
# when real git ingestion lands: GitPython
